/**
 * Classes.
 */

// TODO build and document.

class User {
	// TODO build the `User` class.

	constructor(...params) {}
}

class CrewMember extends User {
	// TODO build the `CrewMember` class.

	constructor(...params) {}
}

class Search {
	constructor(...params) {}
}
